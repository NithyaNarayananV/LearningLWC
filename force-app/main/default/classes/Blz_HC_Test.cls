@isTest
private class Blz_HC_Test {
    @isTest static void myTest() {
        // code_block
        Blz_HealthCheck b = new Blz_HealthCheck();
        b.execute(null);
        System.assertEquals(null, null);
    }
}
